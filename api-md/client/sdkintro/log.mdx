---
title: 日志相关
hide_title: true
sidebar_position: 11
---

<Tabs
groupId="sdks-language"
values={[
{ label: 'Android', value: 'android', },
{ label: 'iOS', value: 'ios', },
{ label: 'JavaScript', value: 'js', },
{ label: 'Flutter', value: 'flutter', },
{ label: 'ReactNative', value: 'reactnative', },
{ label: '鸿蒙', value: 'harmony', }
]
}>
<TabItem value="android">

SDK 默认不在 ADB 输出日志，如果在开发阶段需要输出日志，可以在 SDK init 的时候传入下面参数。

```java
JIM.InitConfig initConfig = new JIM.InitConfig.Builder()
                .setJLogConfig(new JLogConfig.Builder(getApplicationContext()).setLogConsoleLevel(JLogLevel.JLogLevelVerbose).build())
                .build();
JIM.getInstance().init(this, "appKey", initConfig);
```

SDK 相关日志可以通过 "JLogger" 关键字进行筛选。


</TabItem>


<TabItem value="ios">

SDK 默认不在控制台输出日志，如果在开发阶段需要输出日志，请在 SDK init 之前设置。

```objectivec
[JIM.shared setConsoleLogLevel:JLogLevelVerbose];
```

SDK 相关日志可以通过 "JLogger" 关键字进行筛选。

</TabItem>
<TabItem value="js">

</TabItem>
<TabItem value="flutter">

</TabItem>
<TabItem value="reactnative">

React Native SDK 日志通过 Metro 控制台输出。在开发环境下，SDK 会输出相关日志信息。

可以通过控制台筛选 "JuggleIM" 关键字来查看 SDK 相关日志。

```typescript
// SDK 会自动在开发环境输出日志
// 可以在 Metro 控制台查看
```

</TabItem>
<TabItem value="harmony">

</TabItem>
</Tabs> 
